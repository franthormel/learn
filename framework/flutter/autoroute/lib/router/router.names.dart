class RoutePaths {
  static const navigation = '/';

  static const arguments = '/arguments';
  static const display = '/display';
  static const limerick = '/limerick';

  static const transitions = '/transitions';
  static const transitionSlideRight = transitions + '-slide-right';
  static const transitionSlideLeft = transitions + '-slide-left';
  static const transitionSlideRightFade = transitions + '-slide-right-fade';
  static const transitionSlideLeftFade = transitions + '-slide-left-fade';
  static const transitionSlideTop = transitions + '-slide-top';
  static const transitionSlideBottom = transitions + '-slide-bottom';
  static const transitionFadeIn = transitions + '-fade-in';
  static const transitionZoomIn = transitions + '-zoom-in';
  static const transitionNone = transitions + '-none';
}
