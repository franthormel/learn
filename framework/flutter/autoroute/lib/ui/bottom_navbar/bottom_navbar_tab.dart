import 'package:flutter/material.dart';

class BottomNavBarTabPage extends StatelessWidget {
  const BottomNavBarTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hi');
  }
}
