import 'package:flutter/material.dart';

import '../display.dart';

class SlideRightPage extends StatelessWidget {
  const SlideRightPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DisplayPage(title: 'Slide Right');
  }
}
