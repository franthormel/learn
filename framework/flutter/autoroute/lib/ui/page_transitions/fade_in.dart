import 'package:flutter/material.dart';

import '../display.dart';

class FadeInPage extends StatelessWidget {
  const FadeInPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DisplayPage(title: 'Fade In');
  }
}
