import 'package:flutter/material.dart';

import '../display.dart';

class SlideRightFadePage extends StatelessWidget {
  const SlideRightFadePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DisplayPage(title: 'Slide Right with Fade');
  }
}
