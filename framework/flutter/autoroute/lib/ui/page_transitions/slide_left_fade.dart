import 'package:flutter/material.dart';

import '../display.dart';

class SlideLeftFadePage extends StatelessWidget {
  const SlideLeftFadePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DisplayPage(title: 'Slide Left with Fade');
  }
}
