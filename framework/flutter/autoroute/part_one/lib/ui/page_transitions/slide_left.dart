import 'package:flutter/material.dart';

import '../display.dart';

class SlideLeftPage extends StatelessWidget {
  const SlideLeftPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DisplayPage(title: 'Slide Left');
  }
}
